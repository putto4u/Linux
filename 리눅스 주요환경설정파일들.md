## 📁 우분투 리눅스의 주요 환경설정 파일 목록 분석

우분투 리눅스 시스템은 대부분의 설정 정보를 텍스트 파일 형태로 저장합니다. 이 설정 파일들을 이해하고 수정하는 것은 시스템 관리의 핵심입니다.

IT 전문가로서 기능별로 분류된 우분투(데비안 계열)의 주요 환경설정 파일 목록과 그 용도를 상세히 정리해 드립니다. 대부분의 설정 파일은 최고 관리자 권한(`sudo`)이 필요합니다.

---

### 1. 🌐 시스템 초기화 및 서비스 설정 파일

시스템 부팅 및 서비스 실행 방식을 제어하는 파일들입니다.

| 파일/디렉터리 | 경로 | 용도 |
| :--- | :--- | :--- |
| **`fstab`** | `/etc/fstab` | **파일 시스템 테이블**입니다. 부팅 시 마운트할 디스크 파티션, 장치, 파일 시스템 타입 등을 정의합니다. |
| **`default`** | `/etc/default/` | 시스템 데몬(Daemon)의 **초기 실행 옵션**을 지정합니다. (예: `/etc/default/grub`는 GRUB 부트로더의 설정을 담고 있습니다.) |
| **`systemd` 설정** | `/etc/systemd/system/` | 서비스(`service`), 타이머(`timer`), 소켓(`socket`) 등 **`systemd` 관리 서비스의 설정 파일**이 위치합니다. |
| **`sysctl.conf`** | `/etc/sysctl.conf` | **커널 파라미터(Kernel Parameter)**를 영구적으로 설정합니다. 네트워크 성능, 메모리 관리 등의 커널 동작 방식을 변경할 때 사용됩니다. |

### 2. 🔌 네트워크 및 방화벽 설정 파일

시스템의 통신 방식과 접근 제어를 담당하는 파일입니다.

| 파일/디렉터리 | 경로 | 용도 |
| :--- | :--- | :--- |
| **`netplan`** | `/etc/netplan/*.yaml` | **최신 우분투 네트워크 설정** 파일입니다. YAML 형식으로 IP 주소, 게이트웨이, DNS 등을 정의합니다. (`ifconfig`의 설정 파일을 대체) |
| **`hosts`** | `/etc/hosts` | **로컬 호스트 이름과 IP 주소의 매핑**을 정의합니다. DNS 서버에 쿼리하기 전에 먼저 확인합니다. |
| **`resolv.conf`** | `/etc/resolv.conf` | **DNS 네임 서버(Name Server)**의 IP 주소를 정의합니다. 시스템이 도메인 이름을 IP 주소로 변환할 때 사용됩니다. |
| **`ufw` 설정** | `/etc/ufw/` | **UFW (Uncomplicated Firewall)** 방화벽의 규칙 및 프로파일 설정 파일이 위치합니다. |

### 3. 👤 사용자 및 보안 관련 설정 파일

사용자 계정, 권한, 인증 방식 등을 관리하는 파일입니다.

| 파일/디렉터리 | 경로 | 용도 |
| :--- | :--- | :--- |
| **`passwd`** | `/etc/passwd` | 사용자 계정의 기본 정보 (사용자명, UID, 기본 쉘 등)를 저장합니다. (비밀번호는 저장되지 않음) |
| **`shadow`** | `/etc/shadow` | **사용자 비밀번호의 암호화된 해시 값** 및 계정 만료 정책을 저장합니다. (root만 읽기 가능) |
| **`group`** | `/etc/group` | 시스템 그룹 목록과 각 그룹에 속한 사용자 목록을 정의합니다. |
| **`sudoers`** | `/etc/sudoers` | `sudo` 명령을 사용할 수 있는 **사용자 및 그룹, 허용 명령어**를 정의합니다. (수정 시 반드시 `visudo` 사용) |
| **`sshd_config`** | `/etc/ssh/sshd_config` | **SSH 서버(sshd)**의 작동 방식(포트 번호, 루트 로그인 허용 여부, 인증 방식 등)을 설정합니다. |

### 4. 🐚 쉘(Shell) 및 환경 설정 파일

사용자 개개인의 작업 환경을 정의하는 파일입니다. 주로 홈 디렉터리(`~`)에 숨김 파일(`.`으로 시작) 형태로 존재합니다.

| 파일/디렉터리 | 경로 | 용도 |
| :--- | :--- | :--- |
| **`.bashrc`** | `~/.bashrc` | **대화형 쉘**이 실행될 때마다 읽어 들여 **쉘 별칭(alias), 함수, 개인 환경 변수** 등을 설정합니다. |
| **`.profile`** | `~/.profile` | 사용자가 **로그인**할 때 단 한 번 읽어 들여 **전역 환경 변수** 등을 설정합니다. |
| **`.bash_profile`** | `~/.bash_profile` | 일부 시스템에서 로그인 시 사용되나, 우분투는 주로 `.profile`을 사용하고 `.bashrc`를 호출합니다. |
| **`environment`** | `/etc/environment` | **시스템 전체**에 적용되는 **전역 환경 변수**를 정의합니다. |

---

**실전 팁:**

대부분의 시스템 설정 파일은 `/etc` 디렉터리 내에 위치하며, 이 파일들을 수정할 때는 반드시 **원본 파일을 백업(`cp filename filename.bak`)** 해두고 작업하는 것이 시스템 오류를 방지하는 좋은 습관입니다.
