## 🖥️ 우분투 리눅스의 주요 환경 변수 확인 방법

환경 변수(Environment Variables)는 쉘이 작동하거나 프로세스가 실행될 때 필요한 시스템 정보를 담고 있는 변수입니다. 리눅스 시스템의 작동 방식과 프로그램 경로 등을 정의하므로 시스템 관리 및 스크립팅에 필수적인 지식입니다.

우분투 터미널에서 **주요 환경 변수들을 확인하는 명령어**와 **핵심 변수 목록**을 안내해 드립니다.

### 1\. ⚙️ 환경 변수 확인 명령어

환경 변수 전체 목록 또는 특정 변수의 값을 확인하는 방법입니다.

| 명령어 | 기능 | 설명 |
| :--- | :--- | :--- |
| **`env`** | **전체 목록 출력** | 현재 쉘 세션에 설정된 모든 환경 변수와 그 값을 출력합니다. |
| **`printenv`** | **전체 목록 또는 특정 변수 출력** | `env`와 유사하며, 인수를 주지 않으면 전체를 출력하고, 인수를 주면 해당 변수의 값만 출력합니다. |
| **`export`** | **전체 목록 출력 및 변수 설정** | `env`와 유사하나, 주로 변수를 생성/수정하거나 외부 프로세스로 내보낼 때 사용됩니다. |

#### 📝 특정 변수 값 확인 방법

특정 변수의 값을 확인하려면 해당 변수 이름 앞에 **`$`** 기호를 붙여 `echo` 명령어를 사용합니다.

```bash
master@u24:~$ echo $PATH
master@u24:~$ echo $SHELL
```

-----

### 2\. 🔑 우분투의 핵심 환경 변수 목록

리눅스 시스템 작동에 중요한 역할을 하는 대표적인 환경 변수들입니다.

| 환경 변수 | 용도 및 저장되는 정보 | 실전 활용 |
| :--- | :--- | :--- |
| **`PATH`** | **실행 파일 검색 경로** | 명령어를 입력했을 때, 쉘이 실행 파일(바이너리)을 찾는 디렉터리 경로 목록입니다. 경로가 `:`로 구분됩니다. |
| **`HOME`** | **사용자의 홈 디렉터리 경로** | `cd ~` 명령 시 이동하는 경로이며, 사용자의 개인 설정 파일(`~/.bashrc` 등)이 위치합니다. |
| **`USER`** / **`LOGNAME`** | **현재 로그인한 사용자 이름** | 현재 쉘 세션을 소유한 사용자 계정의 이름입니다. |
| **`SHELL`** | **사용자가 사용하는 쉘 경로** | 현재 세션에서 사용 중인 기본 쉘 프로그램의 경로입니다. (대부분 `/bin/bash` 또는 `/bin/zsh`) |
| **`PWD`** | **현재 작업 디렉터리** | `pwd` 명령의 출력과 같습니다. 현재 작업 중인 디렉터리의 절대 경로를 저장합니다. |
| **`LANG`** | **시스템 언어 설정** | 시스템 및 애플리케이션이 사용할 언어 및 인코딩 정보를 정의합니다. (예: `ko_KR.UTF-8`) |
| **`TERM`** | **터미널 타입** | 현재 사용 중인 터미널 에뮬레이터의 종류를 나타냅니다. (예: `xterm`, `xterm-256color`) |
| **`EDITOR`** / **`VISUAL`** | **기본 텍스트 편집기** | `crontab -e`나 `visudo`와 같은 명령 실행 시 기본으로 사용할 편집기를 지정합니다. (`vi`, `vim`, `nano` 등) |
| **`PS1`** | **쉘 프롬프트 모양** | 터미널에 표시되는 프롬프트의 모양(사용자명, 호스트명, 현재 경로 등)을 정의하는 문자열입니다. |
| **`HOSTNAME`** | **시스템의 호스트 이름** | 현재 리눅스 시스템에 설정된 호스트 이름입니다. |

### 3\. 📝 환경 변수 설정 파일 (실전 팁)

환경 변수는 사용자의 편의를 위해 설정 파일에 정의되어 자동으로 로드됩니다.

| 설정 파일 | 경로 | 적용 시점 | 적용 범위 |
| :--- | :--- | :--- | :--- |
| **`/etc/environment`** | `/etc/environment` | **시스템 전체 부팅 시** | 모든 사용자 및 모든 프로세스 (전역) |
| **`.profile`** | `~/.profile` | **사용자 로그인 시** | 로그인한 사용자 (주로 환경 변수 설정) |
| **`.bashrc`** | `~/.bashrc` | **새 쉘 실행 시** | 해당 사용자 (주로 alias, 함수, PATH 추가) |

**실전 팁:** 만약 **PATH 환경 변수**에 새로운 경로를 추가하고 싶다면, 보통 `~/.bashrc` 파일에 다음과 같은 구문을 추가하고 `source ~/.bashrc` 명령으로 적용합니다.

```bash
# ~/.bashrc 파일 내용 예시
export PATH="$HOME/bin:$PATH"
```

이 코드는 홈 디렉터리 내의 `bin` 디렉터리를 기존 `PATH` 목록의 맨 앞에 추가합니다.

---
## 🖥️ 우분투 리눅스 심화 환경 변수 목록 분석

이전에 안내해 드린 기본 환경 변수 외에, 시스템 관리, 보안, 로깅, 그리고 프로그램 실행 환경에 깊숙이 관여하는 **심화 환경 변수**들을 추가로 정리해 드립니다. 이 변수들은 시스템의 작동 방식을 세밀하게 제어할 때 유용합니다.

### 1. ⚙️ 시스템 및 로깅 관련 변수

| 환경 변수 | 용도 및 저장되는 정보 | 실전 활용 및 중요도 |
| :--- | :--- | :--- |
| **`UID`** | **사용자 ID (User ID)** | 현재 사용자의 고유 식별 번호. 0은 `root`를 의미합니다. 보안 검사나 스크립트에서 권한 확인 시 사용됩니다. |
| **`EUID`** | **실효 사용자 ID (Effective UID)** | 현재 실행 중인 프로세스의 실질적인 권한을 나타내는 ID입니다. `sudo` 실행 시 `UID`는 일반 사용자이지만, `EUID`는 `0` (root)이 됩니다. |
| **`MAIL`** | **사용자의 메일함 경로** | 사용자의 로컬 시스템 메일이 저장되는 파일 경로입니다. (예: `/var/mail/username`) |
| **`HISTSIZE`** | **기록할 명령어 줄 수** | **쉘(Bash) 기록 파일(`~/.bash_history`)**에 저장할 명령어의 최대 개수를 정의합니다. |
| **`HISTFILESIZE`** | **기록 파일의 최대 크기** | `~/.bash_history` 파일이 가질 수 있는 최대 줄 수를 정의합니다. (로그인 시 로드, 로그아웃 시 저장) |
| **`TMOUT`** | **자동 로그아웃 시간** | 터미널에 아무 입력이 없을 경우 자동으로 로그아웃되는 시간(초)을 설정합니다. **보안 강화**에 중요합니다. |

### 2. 🔗 라이브러리 및 실행 경로 관련 변수

개발 환경이나 특정 애플리케이션의 실행 경로를 제어하는 데 매우 중요합니다.

| 환경 변수 | 용도 및 저장되는 정보 | 실전 활용 및 중요도 |
| :--- | :--- | :--- |
| **`LD_LIBRARY_PATH`** | **동적 라이브러리 검색 경로** | 프로그램 실행 시, `/lib`나 `/usr/lib`가 아닌 **사용자 정의 경로**에서 공유 라이브러리(`.so` 파일)를 찾도록 지정하는 경로입니다. **개발 및 테스트 환경**에서 자주 사용됩니다. |
| **`MANPATH`** | **매뉴얼 페이지 검색 경로** | `man` 명령어를 실행했을 때, 시스템이 매뉴얼 파일을 찾는 디렉터리 경로 목록입니다. |
| **`CLASSPATH`** | **자바 클래스 검색 경로** | Java 환경에서 JVM(Java Virtual Machine)이 `.class` 파일을 찾는 디렉터리나 압축 파일(JAR)의 경로를 지정합니다. |

### 3. 🌐 네트워크 및 터미널 제어 변수

SSH, 터미널 환경, 로케일 등에 영향을 미치는 변수입니다.

| 환경 변수 | 용도 및 저장되는 정보 | 실전 활용 및 중요도 |
| :--- | :--- | :--- |
| **`SSH_CLIENT`** | **SSH 클라이언트 정보** | 현재 접속한 SSH 클라이언트의 IP 주소, 포트 번호 등을 담고 있습니다. (SSH 접속 시에만 존재) |
| **`SSH_TTY`** | **터미널 장치명** | 현재 SSH 세션에 연결된 **가상 터미널 장치 파일**의 경로입니다. (예: `/dev/pts/0`) |
| **`LC_ALL`** | **강제 로케일 설정** | `LANG`이나 다른 `LC_*` 변수들을 **무시**하고 시스템 전체의 로케일(언어/인코딩)을 **강제**하는 변수입니다. |
| **`DISPLAY`** | **X 윈도우 디스플레이 위치** | X 윈도우 시스템(GUI) 환경에서 애플리케이션이 표시될 디스플레이의 위치를 지정합니다. (SSH 터널링/포워딩 시 필요) |

### 4. 🔗 기타 유용한 변수

| 환경 변수 | 용도 및 저장되는 정보 | 실전 활용 및 중요도 |
| :--- | :--- | :--- |
| **`OLDPWD`** | **이전 작업 디렉터리** | `cd -` 명령을 실행했을 때 되돌아가는, 직전에 작업했던 디렉터리 경로를 저장합니다. |
| **`HISTCONTROL`** | **명령어 기록 제어** | `erasedups` (중복된 명령 제거)나 `ignorespace` (앞에 공백이 있는 명령은 기록하지 않음) 등의 옵션으로 명령어 기록 방식을 제어합니다. |
| **`IFS`** | **내부 필드 구분자** | 쉘이 문자열을 단어(토큰)로 분리할 때 사용하는 구분 문자(기본값: 공백, 탭, 개행)를 정의합니다. 스크립트 작성 시 중요합니다. |

이러한 심화 환경 변수들은 시스템의 미묘한 동작을 이해하고, 특정 애플리케이션의 실행 환경을 정확하게 조정하는 데 필수적입니다.
