## 📝 Vi/Vim 명령어 모음: IT 전문가를 위한 필수 가이드

Vi(Visual Editor)와 Vim(Vi IMproved)은 리눅스/유닉스 환경에서 가장 강력하고 널리 사용되는 텍스트 편집기입니다. 서버 환경에서는 GUI가 없는 경우가 많기 때문에, Vim 사용법을 숙달하는 것은 시스템 관리 및 개발에 필수적인 역량입니다.

Vim은 크게 세 가지 모드(Mode)로 작동하며, 모드별로 핵심 명령어를 정리했습니다.

---

### 1. ⚙️ 모드 전환 (Mode Switching)

Vim을 사용하는 첫 번째 단계는 원하는 작업에 맞게 모드를 전환하는 것입니다.

| 시작 모드 | 전환 명령어 | 도착 모드 | 설명 |
| :--- | :--- | :--- | :--- |
| **Normal** | `i` | **Insert** | 현재 커서 위치 **앞**에 입력 시작 (Insert Before) |
| **Normal** | `a` | **Insert** | 현재 커서 위치 **뒤**에 입력 시작 (Append) |
| **Normal** | `o` | **Insert** | 현재 줄 **다음 줄**에 새 줄을 삽입하고 입력 시작 |
| **Normal** | `v` | **Visual** | 블록 선택 시작 (문자 단위) |
| **Insert, Visual, Command** | **`ESC`** | **Normal** | 항상 **Normal 모드**로 돌아가는 핵심 키 |

---

### 2. ⌨️ Normal Mode (정상 모드) 명령어

파일을 탐색, 수정, 삭제, 복사하는 **핵심 제어 모드**입니다. 파일에 텍스트를 입력할 수 없습니다.

#### A. 커서 이동 및 탐색

| 명령어 | 기능 | 설명 |
| :--- | :--- | :--- |
| `h`, `j`, `k`, `l` | **좌, 하, 상, 우 이동** | 방향키 대신 사용하며 Vim의 기본 이동 방식입니다. |
| `w`, `b` | **단어 이동** (Word, Backward) | `w`: 다음 단어 시작 지점, `b`: 이전 단어 시작 지점으로 이동. |
| `^` 또는 `0` | **줄 시작** | 줄의 맨 처음(0) 또는 공백이 아닌 문자열 시작 지점(`^`)으로 이동. |
| `$` | **줄 끝** | 현재 줄의 맨 끝으로 이동. |
| `gg` | **파일 맨 처음** | 문서의 첫 번째 줄로 이동. |
| `G` | **파일 맨 끝** | 문서의 마지막 줄로 이동. |
| `[수]gg` 또는 `[수]G` | **특정 줄 이동** | 예를 들어, `10G`는 10번째 줄로 이동. |

#### B. 수정 및 삭제

| 명령어 | 기능 | 설명 |
| :--- | :--- | :--- |
| `x` | **한 문자 삭제** | 현재 커서가 위치한 문자를 삭제 (Delete Char) |
| `dd` | **현재 줄 삭제** | 현재 줄 전체를 삭제 (커팅)합니다. |
| `dw` | **단어 삭제** | 현재 커서부터 단어 끝까지 삭제. |
| `D` | **줄 끝까지 삭제** | 현재 커서부터 줄 끝까지 삭제. |
| `[수]dd` | **여러 줄 삭제** | 예: `5dd`는 현재 줄 포함 5줄 삭제. |
| `r` | **한 문자 대체** (Replace) | 다음 입력하는 한 문자로 커서 위치의 문자를 교체합니다. |
| `u` | **되돌리기** (Undo) | 가장 최근의 변경 사항을 취소합니다. |
| `Ctrl + r` | **다시 실행** (Redo) | 되돌린 내용을 다시 실행합니다. |

#### C. 복사 및 붙여넣기 (Yank and Put)

| 명령어 | 기능 | 설명 |
| :--- | :--- | :--- |
| `yy` | **현재 줄 복사** (Yank) | 현재 줄 전체를 복사 (클립보드에 저장)합니다. |
| `[수]yy` | **여러 줄 복사** | 예: `3yy`는 현재 줄 포함 3줄 복사. |
| `p` | **붙여넣기** (Put) | 복사된 내용을 현재 커서 **다음 줄**에 붙여넣습니다. |
| `P` | **붙여넣기** (Put) | 복사된 내용을 현재 커서 **현재 줄**에 붙여넣습니다. |

### 3. ⌨️ Insert Mode (입력 모드) 명령어

텍스트를 실제로 입력하는 모드입니다. 일반적인 텍스트 편집기와 같습니다.

| 명령어 | 기능 | 설명 |
| :--- | :--- | :--- |
| `ESC` | **Normal 모드로 전환** | 입력이 끝나면 반드시 `ESC`를 눌러 Normal 모드로 돌아가야 합니다. |

### 4. ⌨️ Command Mode (명령 모드) 명령어

Normal 모드에서 **`:`**를 입력하여 진입합니다. 파일을 저장하거나, 종료하거나, 검색 및 대체 등의 고급 기능을 수행합니다.

| 명령어 | 기능 | 설명 |
| :--- | :--- | :--- |
| `:w` | **저장** (Write) | 파일 내용을 저장합니다. |
| `:q` | **종료** (Quit) | 파일 내용을 저장하지 않았으면 종료되지 않습니다. |
| `:wq` | **저장 후 종료** | 파일 저장 후 Vim을 종료합니다. |
| `:q!` | **강제 종료** | 파일 내용을 저장하지 않고 강제로 종료합니다. (변경 사항 손실) |
| `:w filename` | **다른 이름으로 저장** | 파일을 `filename`으로 저장합니다. |
| `:set nu` | **줄 번호 표시** | 편집기 왼쪽에 줄 번호를 표시합니다. `:set nonu`로 해제. |
| `:! [명령어]` | **쉘 명령어 실행** | Vim을 벗어나지 않고 쉘 명령을 실행합니다. 예: `:!ls -al` |

#### A. 검색 및 대체 (Search & Replace)

| 명령어 | 기능 | 설명 |
| :--- | :--- | :--- |
| `/text` | **텍스트 검색** | 커서 위치부터 아래로 `text`를 검색합니다. |
| `n` | **다음 검색 결과** | 다음 일치 항목으로 이동. |
| `N` | **이전 검색 결과** | 이전 일치 항목으로 이동. |
| `:%s/찾을_문자열/바꿀_문자열/g` | **전체 대체** | 파일 전체(`%`)에서 `g` (Global) 옵션을 사용하여 해당 문자열을 모두 바꿉니다. |
| `:%s/old/new/gc` | **전체 대체 및 확인** | 대체 시마다 사용자에게 `c` (Confirm) 확인을 요청합니다. |
