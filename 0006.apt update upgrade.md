## 패키지 목록 업데이트의 이해: `apt update`

데비안(Debian) 계열의 리눅스(Ubuntu, Kali Linux 등) 시스템을 관리할 때 가장 먼저 접하게 되는 명령어가 바로 `apt update`입니다. 이 명령어의 정확한 역할과 동작 원리를 이해하는 것은 안정적인 서버 운영의 기초가 됩니다.

---

### 1. `apt update`의 정의 및 역할

`apt update`는 시스템에 설치된 소프트웨어를 **직접 업데이트하는 명령어가 아닙니다.** 대신, 내 컴퓨터가 참조하고 있는 **패키지 저장소(Repository)**로부터 최신 소프트웨어의 **목록(색인) 정보**를 가져와 로컬 데이터베이스를 갱신하는 역할을 합니다.

* **동작 원리**: `/etc/apt/sources.list` 파일에 등록된 서버 주소에 접속하여 최신 패키지 리스트 파일(Metadata)을 다운로드합니다.
* **저장 위치**: 다운로드된 정보는 `/var/lib/apt/lists/` 디렉토리에 저장됩니다.
* **목적**: 현재 내 시스템에 설치된 버전보다 더 최신 버전이 있는지, 혹은 새로 설치할 수 있는 패키지가 무엇인지 확인하기 위함입니다.

### 2. `update`와 `upgrade`의 결정적 차이

많은 사용자가 혼동하는 두 명령어의 차이점을 표로 정리하면 다음과 같습니다.

| 구분 | `apt update` | `apt upgrade` |
| --- | --- | --- |
| **핵심 기능** | 패키지 **목록** 동기화 | 실제 패키지 **설치 및 업데이트** |
| **데이터 변경** | 로컬 인덱스(DB)만 갱신 | 실제 프로그램 파일 교체 |
| **실행 순서** | 항상 먼저 실행 (1순위) | `update` 완료 후 실행 (2순위) |
| **비용 발생 여부** | 데이터 전송량 외 추가 비용 없음 | 서비스 중단 가능성 및 디스크 용량 사용 |

> **중요**: `apt update`를 생략하고 `apt upgrade`를 실행하면, 시스템은 최신 버전이 출시되었는지 알지 못하므로 아무런 업데이트도 수행하지 않거나 구버전 목록을 기준으로 동작하게 됩니다.

---

### 3. 명령어 실행 및 주요 메시지 분석

터미널에서 `sudo apt update`를 입력했을 때 나타나는 주요 상태값의 의미입니다.

* **Hit (적중)**: 로컬에 저장된 패키지 목록이 저장소의 목록과 동일하여 업데이트가 필요 없는 상태입니다.
* **Get (가져오기)**: 저장소에 새로운 정보가 있어 최신 패키지 목록을 다운로드 중인 상태입니다.
* **Ign (무시)**: 패키지 목록이 너무 최신이거나, 사소한 변경이어서 무시해도 되는 상태입니다.

#### 실행 예시

```bash
$ sudo apt update
Hit:1 http://kr.archive.ubuntu.com/ubuntu jammy InRelease
Get:2 http://kr.archive.ubuntu.com/ubuntu jammy-updates InRelease [119 kB]
...
Reading package lists... Done
Building dependency tree... Done
Reading state information... Done
62 packages can be upgraded. Run 'apt list --upgradable' to see them.

```

위 메시지 마지막 줄의 **"62 packages can be upgraded"**는 `update`를 통해 내 시스템보다 최신 버전인 패키지가 62개 있음을 발견했다는 뜻입니다.

---

### 4. 주의사항 및 팁

* **관리자 권한**: 시스템의 패키지 목록을 수정하므로 반드시 `sudo` 권한이 필요합니다.
* **네트워크 연결**: 저장소 서버에 접속해야 하므로 인터넷 연결이 필수적입니다.
* **권장 사용 패턴**: 새로운 패키지를 설치(`apt install`)하기 직전이나, 시스템 전체를 최신화하기 전에 습관적으로 실행하는 것이 좋습니다.

---

**Next Step: `apt upgrade`의 종류와 의존성 해결**

[Update vs Upgrade 차이점](https://www.youtube.com/watch?v=oIChHDtb8cs)
이 영상은 리눅스 패키지 매니저의 작동 방식과 `/etc/apt/sources.list` 설정 파일의 관계를 시각적으로 잘 설명해 줍니다.
