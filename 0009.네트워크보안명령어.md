## 🌐 네트워크 및 네트워크 보안 관련 핵심 명령어 모음

네트워크 인프라를 진단하고 보안 상태를 확인하는 것은 IT 전문가의 필수 업무입니다. 우분투 리눅스 환경에서 자주 사용되는 **네트워크 진단 및 보안 관련 핵심 명령어**들을 기능별로 정리해 드립니다.

### 1. 🌐 네트워크 상태 확인 및 진단 (Diagnosis)

시스템의 현재 네트워크 연결 상태와 경로를 파악합니다.

| 명령어 | 기능 | 사용 구문 | 설명 및 활용 |
| :--- | :--- | :--- | :--- |
| **`ip`** | **네트워크 인터페이스 및 라우팅 확인** | `ip a` (IP 주소 확인), `ip r` (라우팅 테이블) | 최신 리눅스의 표준 네트워크 관리 도구. IP 주소, NIC 상태, 경로 등 모든 정보를 다룹니다. |
| **`ping`** | **네트워크 연결 테스트** | `ping [IP 주소 or 도메인]` | 원격 호스트까지의 **기본 연결성(ICMP)**을 확인하고 패킷 손실률과 지연 시간을 측정합니다. |
| **`traceroute`** | **경로 추적** | `traceroute [IP 주소 or 도메인]` | 목적지까지 가는 **네트워크 경로(Hop)**와 각 라우터에서의 지연 시간을 확인하여 병목 지점을 찾습니다. |
| **`ss`** | **소켓 상태 확인** | `ss -antp` | **열려있는 포트, 연결 상태(LISTEN, ESTABLISHED),** 해당 연결을 사용 중인 **프로세스(PID)**를 상세히 보여줍니다. (`netstat`의 대체 명령어) |
| **`dig`** | **DNS 정보 조회** (Domain Information Groper) | `dig [도메인]` | 도메인 이름에 대한 IP 주소(A 레코드), 메일 서버(MX), 네임 서버(NS) 등 **DNS 레코드 정보**를 상세히 질의합니다. |
| **`host`** | **간단한 DNS 조회** | `host [도메인]` | 도메인 이름에 대한 IP 주소를 빠르게 조회합니다. |

### 2. 🛡️ 네트워크 보안 및 방화벽 제어 (Security & Firewall)

시스템의 접근 제어 및 보안 상태를 설정하고 확인합니다.

| 명령어 | 기능 | 사용 구문 | 설명 및 활용 |
| :--- | :--- | :--- | :--- |
| **`ufw`** | **방화벽 관리** (Uncomplicated Firewall) | `sudo ufw status`, `sudo ufw allow 22/tcp` | 우분투에서 사용하기 쉬운 **iptables 프런트엔드**입니다. 포트 허용/차단 규칙을 설정하고 상태를 확인합니다. |
| **`iptables`** | **커널 방화벽 제어** | `sudo iptables -L -v` | 리눅스 커널의 **패킷 필터링 규칙**을 직접 설정하고 관리합니다. 복잡하지만 강력한 제어 기능을 제공합니다. |
| **`tcpdump`** | **패킷 캡처** | `sudo tcpdump -i eth0 port 80` | 네트워크 인터페이스를 통과하는 **패킷을 가로채서(Sniffing) 내용을 분석**합니다. 네트워크 문제 및 보안 침입 분석에 사용됩니다. |
| **`nmap`** | **포트 스캐닝 및 서비스 분석** | `nmap [IP 주소]` | 대상 호스트의 **열려있는 포트**를 스캔하고, 운영체제(OS) 및 실행 중인 서비스의 버전 정보를 알아냅니다. (보안 진단에 필수) |
| **`netcat`** (`nc`) | **TCP/UDP 연결 및 데이터 전송** | `nc -vz [IP 주소] [포트]` | 포트가 열려있는지 확인하거나, 간단한 클라이언트/서버 통신을 구축하여 **네트워크 연결을 테스트**합니다. |

### 3. 🔑 보안 연결 및 접속 명령어

안전한 원격 접속 및 파일 전송에 사용되는 명령어입니다.

| 명령어 | 기능 | 사용 구문 | 설명 및 활용 |
| :--- | :--- | :--- | :--- |
| **`ssh`** | **원격 보안 접속** | `ssh [user]@[host]` | 암호화된 터널을 통해 원격 서버에 접속하여 쉘 환경을 사용합니다. |
| **`scp`** | **원격 파일 복사** (Secure Copy) | `scp local_file user@host:remote_path` | SSH를 기반으로 안전하게 로컬과 원격 간의 파일을 복사합니다. |
| **`sftp`** | **보안 파일 전송** (Secure FTP) | `sftp user@host` | SSH를 기반으로 대화식(Interactive)으로 원격 서버의 파일을 관리하고 전송합니다. |

### 4. 📝 설정 파일 관리

네트워크 관련 핵심 설정 파일들을 관리합니다.

| 명령어 | 기능 | 사용 구문 | 설명 및 활용 |
| :--- | :--- | :--- | :--- |
| **`vi/nano`** | **설정 파일 수정** | `sudo nano /etc/netplan/01-netcfg.yaml` | `netplan` 설정 파일, `/etc/hosts`, `/etc/resolv.conf` 등 네트워크 설정 파일을 직접 편집합니다. |
| **`systemctl`** | **네트워크 서비스 관리** | `sudo systemctl restart NetworkManager` | 네트워크 관리 서비스(NetworkManager, systemd-networkd 등)를 재시작하여 변경된 설정을 적용합니다. |

이 명령어들은 네트워크 관리 및 보안 진단을 위한 기본 도구들이며, 대부분의 상황에서 문제 해결의 실마리를 제공합니다.
