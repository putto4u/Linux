## ⚙️ 리눅스 프로세스 관리 핵심 명령어 모음

리눅스 시스템에서 프로세스(Process)는 시스템 자원을 사용하고 작업을 수행하는 기본 단위입니다. 프로세스를 확인, 모니터링, 제어하는 것은 시스템 관리의 가장 기본적인 업무입니다.

앞서 안내해 드린 PID 관련 명령어 외에, 프로세스 상태 관리 및 제어에 필요한 핵심 명령어들을 기능별로 분류하여 정리해 드립니다.

### 1. 🔍 프로세스 상태 및 목록 확인

현재 시스템에서 실행 중인 프로세스를 다양한 방식으로 확인하고 검색합니다.

| 명령어 | 기능 | 사용 구문 | 설명 및 옵션 |
| :--- | :--- | :--- | :--- |
| **`ps`** | **프로세스 상태 확인** (Process Status) | `ps aux` | 시스템의 모든 프로세스를 상세하게 출력합니다. **`a`**: 터미널과 관계없이, **`u`**: 사용자 기반 형식, **`x`**: 제어 터미널 없는 프로세스 포함. |
| **`top`** | **실시간 모니터링** | `top` | CPU, 메모리, 프로세스 목록을 **실시간으로 갱신**하며 보여줍니다. 부하 발생 시 진단에 가장 유용합니다. |
| **`htop`** | **대화형 모니터링** | `htop` | `top`보다 시각적이고 사용자 친화적인 도구입니다. **설치 필요** (`sudo apt install htop`). |
| **`pgrep`** | **프로세스 이름으로 PID 찾기** | `pgrep [이름]` | 특정 이름(`nginx`, `ssh` 등)을 가진 프로세스의 **PID**를 즉시 출력합니다. |
| **`lsof`** | **열린 파일 확인** (List Open Files) | `lsof -i :80` | 프로세스가 **열고 있는 파일, 소켓, 포트 연결** 목록을 보여줍니다. 포트 충돌 및 네트워크 연결 디버깅에 필수입니다. |

### 2. 🛑 프로세스 제어 및 종료

PID를 이용하여 프로세스에 시그널을 보내 종료, 중단, 또는 재시작을 요청합니다.

| 명령어 | 기능 | 사용 구문 | 설명 및 시그널 |
| :--- | :--- | :--- | :--- |
| **`kill`** | **PID에 시그널 전송** | `kill [PID]` | 기본적으로 `SIGTERM` (15) 시그널을 보내 **정상 종료를 요청**합니다. |
| **`kill -9`** | **강제 종료** | `kill -9 [PID]` | `SIGKILL` (9) 시그널을 보내 **즉시 강제 종료**합니다. 데이터 손실 위험으로 최후의 수단으로 사용합니다. |
| **`killall`** | **이름으로 일괄 종료** | `killall [이름]` | PID를 알 필요 없이 해당 이름의 **모든 프로세스**를 종료합니다. |
| **`pkill`** | **조건부 종료** | `pkill -u [사용자]` | 특정 사용자나 터미널 등 **조건을 만족하는** 프로세스에 시그널을 보냅니다. |
| **`renice`** | **프로세스 우선순위 변경** | `sudo renice -5 [PID]` | 프로세스의 실행 우선순위(**Nice 값**)를 변경하여 CPU 할당량을 조절합니다. (음수: 우선순위 높임) |

### 3. ⏳ 백그라운드 및 작업 관리

프로세스를 터미널 세션과 분리하여 백그라운드에서 실행하고 관리합니다.

| 명령어 | 기능 | 사용 구문 | 설명 및 옵션 |
| :--- | :--- | :--- | :--- |
| **`&`** | **백그라운드 실행** | `명령어 &` | 명령을 실행함과 동시에 **터미널 제어권을 즉시 반환**하여 사용자가 다른 작업을 할 수 있게 합니다. |
| **`jobs`** | **백그라운드 작업 목록** | `jobs` | 현재 터미널에서 백그라운드로 실행 중인 작업 목록을 확인합니다. |
| **`bg`** | **백그라운드로 전환** | `bg %1` | 일시 중지된 작업(PID)을 **백그라운드**에서 계속 실행합니다. |
| **`fg`** | **포어그라운드로 전환** | `fg %1` | 백그라운드 작업을 **포어그라운드**로 불러와 터미널에서 제어합니다. |
| **`nohup`** | **세션 종료 무시** | `nohup 명령어 &` | 현재 터미널 세션이 종료(로그아웃)되어도 프로세스가 종료되지 않고 계속 백그라운드에서 실행되도록 합니다. |
| **`screen` / `tmux`** | **영구 세션 관리** | `screen` 또는 `tmux` | 터미널을 다중화하여 연결이 끊어져도 백그라운드에서 세션을 유지할 수 있는 전문 도구입니다. |

### 4. 🔗 서비스 관리 (Systemd 환경)

우분투를 포함한 최신 리눅스 시스템은 `systemd`를 사용하여 서비스를 관리합니다.

| 명령어 | 기능 | 사용 구문 | 설명 |
| :--- | :--- | :--- | :--- |
| **`systemctl status`** | **서비스 상태 확인** | `systemctl status sshd` | 특정 서비스(`sshd`, `nginx` 등)가 실행 중인지, PID는 무엇인지 등을 상세히 확인합니다. |
| **`systemctl start/stop/restart`** | **서비스 제어** | `systemctl restart nginx` | 서비스를 정상적으로 시작, 중지, 또는 재시작합니다. |
| **`systemctl enable/disable`** | **자동 시작 설정** | `systemctl enable nginx` | 시스템 부팅 시 해당 서비스를 자동으로 시작하도록 설정합니다. |

이 명령어들을 익히면 시스템 자원을 효율적으로 관리하고 문제 발생 시 신속하게 대처할 수 있습니다. 
