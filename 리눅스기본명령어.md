## 🐧 리눅스 기본 명령어 모음: IT 전문가를 위한 핵심 가이드

리눅스 시스템 관리, 개발, 및 디버깅 작업을 위해 반드시 숙지해야 할 핵심 기본 명령어들을 기능별로 분류하여 정리했습니다.

---

### 1. 📂 파일 및 디렉터리 관리 명령어

가장 기본이 되며, 시스템의 파일 구조를 탐색하고 관리하는 데 사용됩니다.

| 명령어 | 기능 | 실전 팁 |
| :--- | :--- | :--- |
| `ls` | **목록 표시** (List) | `ls -alF`를 습관처럼 사용하세요 (`a`: 숨김 파일 포함, `l`: 상세 정보, `F`: 파일 타입 표시). |
| `cd` | **디렉터리 변경** (Change Directory) | `cd ~` (홈 디렉터리), `cd -` (이전 디렉터리) 이동은 필수입니다. |
| `pwd` | **현재 디렉터리 표시** (Print Working Directory) | 현재 위치를 절대 경로로 정확히 확인할 때 사용합니다. |
| `mkdir` | **디렉터리 생성** (Make Directory) | `mkdir -p a/b/c`를 사용하면 부모 디렉터리가 없어도 한 번에 생성합니다. |
| `rmdir` | **디렉터리 삭제** (Remove Directory) | 내용물이 없는 빈 디렉터리만 삭제 가능합니다. |
| `cp` | **파일 복사** (Copy) | `cp -r`은 디렉터리를 재귀적으로 복사할 때 사용합니다. |
| `mv` | **파일 이동/이름 변경** (Move) | `mv old_name new_name`처럼 파일 이름을 변경하는 용도로도 자주 사용됩니다. |
| `rm` | **파일 및 디렉터리 삭제** (Remove) | `rm -rf`는 매우 강력하니 **사용에 극도로 주의**해야 합니다. |

### 2. 📝 파일 내용 확인 및 편집 명령어

로그 파일 확인, 설정 파일 검토, 간단한 텍스트 편집 등에 필수적인 명령어입니다.

| 명령어 | 기능 | 실전 팁 |
| :--- | :--- | :--- |
| `cat` | **파일 내용 전체 출력** (Concatenate) | 여러 파일을 연결하여 출력하거나 `Here Document`로 입력받을 때 유용합니다. |
| `less` | **파일 내용 페이지 단위 출력** | 대용량 로그 파일을 읽을 때 사용하며, `q`로 종료, `/`로 검색합니다. |
| `tail` | **파일 끝부분 출력** | `tail -f /var/log/syslog`는 **실시간 로그 모니터링**에 필수입니다. |
| `head` | **파일 시작 부분 출력** | `head -n 10 filename`으로 처음 10줄만 빠르게 확인할 수 있습니다. |
| `touch` | **빈 파일 생성 또는 타임스탬프 갱신** | 파일의 최종 접근/수정 시간을 현재 시각으로 변경할 때 사용합니다. |
| `grep` | **텍스트 검색 필터링** | `grep -r "error" .` (현재 디렉터리에서 'error' 문자열 재귀 검색)은 디버깅의 핵심입니다. |
| `find` | **파일 시스템에서 파일 찾기** | `find /home -name "*.log"` (로그 파일 찾기)처럼 강력한 검색 필터를 제공합니다. |
| `vi` / `vim` | **텍스트 편집기** | 서버 환경에서 가장 흔히 사용되는 편집기로, 익숙해져야 합니다. |

### 3. 🌐 네트워크 및 시스템 정보 명령어

시스템 상태 확인, 네트워크 문제 진단, 프로세스 모니터링에 사용됩니다.

| 명령어 | 기능 | 실전 팁 |
| :--- | :--- | :--- |
| `ip` | **네트워크 설정 및 상태 확인** | `ip a` (IP 주소), `ip r` (라우팅 테이블) 등 **`ifconfig`를 대체**하는 최신 명령어입니다. |
| `ping` | **네트워크 연결 상태 확인** | 다른 호스트와의 기본 연결(ICMP)을 테스트합니다. |
| `ss` | **소켓 상태 확인** | `ss -antp`로 **열려있는 포트**와 해당 **프로세스**를 확인합니다. (`netstat`의 대체 명령어) |
| `curl` / `wget` | **데이터 전송** | 웹 서버로부터 파일을 다운로드하거나 API 테스트를 할 때 사용합니다. |
| `ps` | **프로세스 상태 확인** | `ps aux`는 시스템의 모든 프로세스를 상세하게 보여줍니다. |
| `top` / `htop` | **실시간 프로세스 모니터링** | CPU 및 메모리 사용량을 실시간으로 확인하는 데 유용합니다. (`htop`이 더 사용자 친화적) |
| `kill` | **프로세스 종료** | `kill -9 [PID]`로 특정 프로세스를 강제로 종료합니다. |
| `df` | **디스크 사용량 확인** | `df -h`는 읽기 쉬운 형식(`h`)으로 디스크 파티션 사용량을 보여줍니다. |
| `du` | **파일 및 디렉터리 공간 확인** | `du -sh folder_name`으로 특정 디렉터리의 크기를 빠르게 확인합니다. |

### 4. 👤 사용자 및 권한 관리 명령어

보안과 시스템 관리를 위해 사용자 및 파일 권한을 제어합니다.

| 명령어 | 기능 | 실전 팁 |
| :--- | :--- | :--- |
| `sudo` | **최고 관리자 권한으로 명령 실행** | `sudo !!` (이전 명령을 sudo로 다시 실행)는 매우 유용한 단축키입니다. |
| `su` | **사용자 전환** (Switch User) | `su -`를 입력하면 환경 변수까지 root로 완전히 전환됩니다. |
| `chmod` | **파일 권한 변경** (Change Mode) | `chmod 755 script.sh` (소유자: 읽기/쓰기/실행, 그룹/기타: 읽기/실행)처럼 숫자로 관리하세요. |
| `chown` | **파일 소유자/그룹 변경** (Change Owner) | `chown -R user:group folder`는 웹 서비스 배포 시 권한 설정에 필수입니다. |
| `adduser` | **사용자 추가** | 새로운 시스템 사용자를 생성합니다. |
| `passwd` | **사용자 비밀번호 변경** | 사용자의 비밀번호를 설정하거나 변경합니다. |
| `whoami` | **현재 로그인된 사용자 표시** | 현재 명령을 실행 중인 사용자가 누구인지 확인합니다. |

---

### 🔑 실전 팁: 파이프와 리디렉션의 활용

기본 명령어를 더욱 강력하게 만들어주는 리눅스의 핵심 기능입니다.

* **파이프 (`|`):** 한 명령어의 출력을 다른 명령어의 입력으로 연결합니다.
    * **예시:** `ps aux | grep nginx` (전체 프로세스 목록을 `grep` 명령어의 입력으로 전달하여 nginx만 필터링)
* **리디렉션 (`>` / `>>`):** 명령어의 출력을 파일로 보내거나 파일의 내용을 명령어로 가져옵니다.
    * `>`: 출력을 파일에 **덮어쓰기**
    * `>>`: 출력을 파일 **끝에 추가**
    * **예시:** `ls -l > file_list.txt` (목록 결과를 파일에 저장)
    * **예시:** `cat file1.txt >> file2.txt` (file1 내용을 file2 끝에 추가)

이 명령어들을 꾸준히 사용하여 시스템 관리에 익숙해지는 것이 중요합니다.
