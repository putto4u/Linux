## 💾 저장 공간, 파일 시스템, 마운트 관련 명령어 모음 (순서 변경 적용)

요청하신 대로 **저장 공간 확인 및 관리**를 먼저 다루고, 이어서 **마운트 및 파일 시스템** 관련 명령어를 정리해 드립니다. 이 명령어들은 리눅스 시스템의 물리적 자원 관리와 데이터 접근 제어에 필수적입니다.

---

### 1. 📊 저장 공간 및 디스크 정보 확인 명령어

디스크의 크기, 사용량, 파티션 정보, IO 성능 등을 확인합니다.

| 명령어 | 기능 | 사용 구문 | 설명 및 활용 |
| :--- | :--- | :--- | :--- |
| **`df`** | **디스크 사용 가능 공간 확인** (Disk Free) | `df -h` | **파일 시스템별** 총 용량, 사용량, 남은 공간을 **읽기 쉬운 형식(`-h`)**으로 출력합니다. 시스템에 용량 부족 경고 시 가장 먼저 확인합니다. |
| **`du`** | **디렉터리 사용 공간 확인** (Disk Usage) | `du -sh /home/user/` | **특정 디렉터리**가 사용하는 총 공간을 요약(`-s`)하여 읽기 쉬운 형태(`-h`)로 출력합니다. (공간 낭비 요소 추적에 사용) |
| **`lsblk`** | **블록 장치 목록 출력** | `lsblk -f` | 시스템에 연결된 모든 **물리적/논리적 블록 장치(디스크, 파티션)** 목록과 그 장치의 **파일 시스템 정보**를 출력합니다.  |
| **`iostat`** | **디스크 입출력 통계** | `iostat -x 1` | 디스크 장치의 **IO 성능** (초당 읽기/쓰기 속도, 대기 시간 등)을 확인하여 하드웨어 병목 현상을 진단합니다. |
| **`fdisk`** | **파티션 관리** (대화형) | `sudo fdisk /dev/sdb` | 디스크(`/dev/sdb` 등)의 **파티션을 생성, 삭제, 수정**할 때 사용되는 대화형 도구입니다. (2TB 미만 권장) |
| **`parted`** | **파티션 관리** (명령줄 지향) | `sudo parted /dev/sdb print` | 2TB 이상의 디스크(GPT 파티션 테이블) 관리에 사용되며, 스크립트 작성에 유리합니다. |

### 2. 📝 파일 시스템 생성 및 유지보수 명령어

파티션에 파일 시스템을 생성하고, 오류를 검사하여 무결성을 유지합니다.

| 명령어 | 기능 | 사용 구문 | 설명 및 활용 |
| :--- | :--- | :--- | :--- |
| **`mke2fs` / `mkfs`** | **파일 시스템 생성** | `sudo mkfs.ext4 /dev/sdb1` | 파티션에 `ext4`, `xfs`, `ntfs` 등 원하는 파일 시스템을 생성(포맷)합니다. (`mkfs`는 통합 명령어) |
| **`fsck`** | **파일 시스템 무결성 검사** (File System Check) | `sudo fsck -A` | 파일 시스템의 오류를 검사하고 복구합니다. (검사 시 해당 파티션은 **언마운트 상태**여야 함) |
| **`e2label`** | **파일 시스템 레이블 설정/확인** | `sudo e2label /dev/sdb1 Data_Drive` | 장치에 사람이 읽을 수 있는 이름(레이블)을 지정하여 `fstab`에서 장치 이름 대신 사용하면 유연성이 높아집니다. |

### 3. 🔗 마운트 및 연결/해제 명령어

파일 시스템을 디렉터리 구조에 연결하고, 사용을 마친 후 분리합니다.

| 명령어 | 기능 | 사용 구문 | 설명 및 활용 |
| :--- | :--- | :--- | :--- |
| **`mount`** | **파일 시스템 연결** | `sudo mount -t ext4 /dev/sdb1 /mnt/data` | 장치와 마운트 지점을 지정하여 연결합니다. 타입(`-t`)이나 옵션(`-o`)을 지정할 수 있습니다. |
| **`umount`** | **마운트 해제** (Unmount) | `sudo umount /mnt/data` | 마운트 지점이나 장치를 지정하여 해제합니다. 해제 전에는 **해당 디렉터리를 사용 중인 프로세스가 없어야** 합니다. |
| **`umount -l`** | **게으른 해제** (Lazy Unmount) | `sudo umount -l /mnt/data` | 장치가 사용 중이더라도 즉시 연결을 끊고, 프로세스가 모두 종료된 후 자원을 완전히 해제합니다. |
| **`fstab`** | **영구 마운트 설정 파일** | `/etc/fstab` (파일 경로) | 시스템 **부팅 시 자동 마운트**를 위한 설정 파일입니다. 수정 후에는 **`sudo mount -a`** 명령으로 테스트해야 합니다. |
| **`findmnt`** | **마운트 정보 트리 구조 출력** | `findmnt` | 현재 마운트된 모든 정보를 트리 구조로 시각화하여 보여줍니다. |

---
**실전 팁:**

대용량 디스크를 추가했을 때의 작업 순서는 다음과 같습니다.
1.  **`lsblk`**로 새 장치 확인.
2.  **`fdisk`** 또는 **`parted`**로 파티션 생성.
3.  **`mkfs.ext4`**로 파일 시스템 생성.
4.  **`mkdir`**로 마운트 지점 생성 (`/mnt/data` 등).
5.  **`mount`** 명령으로 임시 마운트하여 정상 작동 확인.
6.  **`/etc/fstab`**에 영구 마운트 설정 추가.
