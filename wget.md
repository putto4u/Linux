## ⬇️ Wget 명령어 완벽 가이드

\*\*`wget`\*\*은 Linux 및 Unix 계열 시스템에서 파일을 다운로드하는 데 사용되는 강력하고 유연한 **명령줄(Command-line)** 유틸리티입니다. "Web Get"의 줄임말로, HTTP, HTTPS, FTP 프로토콜을 통해 웹상에 있는 파일을 사용자 개입 없이(비대화형) 직접 서버로 다운로드할 수 있습니다.

주로 서버 관리, 스크립트 자동화, 대용량 파일 다운로드, 웹사이트 미러링 등에 필수적으로 사용됩니다.

-----

## 1\. 📝 Wget 기본 구문 및 사용법

기본적인 사용법은 옵션 없이 다운로드할 파일의 URL만 입력하는 것입니다. 파일은 현재 작업 디렉토리에 저장됩니다.

$$\text{wget} \ [\text{옵션}] \ [\text{URL}]$$

### A. 기본 다운로드

```bash
# 파일이 현재 디렉토리에 다운로드되며, 원본 파일 이름으로 저장됩니다.
wget https://example.com/files/archive.zip
```

### B. 다른 이름으로 저장 (`-O`)

다운로드할 파일을 원하는 이름으로 저장합니다.

```bash
# archive.zip 파일을 my_new_archive.zip 이라는 이름으로 저장
wget -O my_new_archive.zip https://example.com/files/archive.zip
```

### C. 특정 디렉토리에 저장 (`-P`)

파일을 저장할 위치(경로)를 지정합니다.

```bash
# 파일을 /home/user/downloads 폴더에 저장
wget -P /home/user/downloads https://example.com/files/archive.zip
```

-----

## 2\. 🛠️ 필수 옵션 및 실전 팁

| 옵션 | 단축형 | 설명 및 용도 | 예시 |
| :--- | :--- | :--- | :--- |
| **재개 (Resume)** | `-c` | **중단된 다운로드**를 이어서 받습니다. 대용량 파일 다운로드 시 네트워크 오류로 끊겨도 처음부터 다시 받을 필요가 없습니다. | `wget -c [URL]` |
| **백그라운드** | `-b` | 다운로드 작업을 **백그라운드**에서 실행합니다. 터미널을 계속 사용할 수 있으며, 진행 상황은 `wget-log` 파일에 기록됩니다. | `wget -b [URL]` |
| **재귀적 다운로드** | `-r` | URL의 링크를 따라가며 하위 디렉토리와 파일들을 **재귀적으로(Recursive)** 모두 다운로드합니다. 웹사이트 미러링의 기본입니다. | `wget -r [URL]` |
| **미러링** | `-m` | 웹사이트 전체를 다운로드하는 미러링 명령입니다. `-r -N -l inf` 등 여러 옵션을 결합한 것입니다. | `wget -m [URL]` |
| **다운로드 속도 제한** | `--limit-rate` | 다운로드 속도를 제한하여 네트워크 대역폭을 조절합니다. | `wget --limit-rate=500k [URL]` |
| **로그 출력 파일** | `-o` | 출력 메시지(로그)를 지정된 파일에 저장합니다. | `wget -o download.log [URL]` |
| **재시도 횟수** | `--tries` | 다운로드 실패 시 재시도할 횟수를 설정합니다. 0 또는 `inf`는 무한 재시도를 의미합니다. | `wget --tries=50 [URL]` |

-----

## 3\. 🌐 웹사이트 전체 미러링 예시

웹사이트의 모든 페이지와 리소스를 로컬에 저장하여 오프라인으로 보려는 경우 자주 사용합니다.

```bash
wget \
--mirror \             # 미러링 모드 활성화 (재귀적, 타임스탬프 기반)
--convert-links \      # 로컬에서 볼 수 있도록 링크를 상대 경로로 변환
--page-requisites \    # HTML 페이지에 필요한 모든 파일(CSS, JS, 이미지) 다운로드
--no-parent \          # 상위 디렉토리로 이동하지 않도록 제한
https://example.com/
```

**[실전 팁]** 웹사이트 미러링 시에는 서버에 과부하를 주지 않도록 `--wait` 옵션을 사용하여 요청 간 지연 시간을 두는 것이 좋습니다.

```bash
wget -m --wait=1 [URL]
```
