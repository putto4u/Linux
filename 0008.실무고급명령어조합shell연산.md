## 리눅스 실무 고급 명령어 조합 및 쉘 연산자 마스터

현업에서 시스템 엔지니어나 개발자가 마주하는 복잡한 상황들을 해결하기 위한 실용적인 명령어 조합과 쉘 연산자의 정수를 정리했습니다. 각 예제는 실제 서버 운영 환경에서 즉시 사용할 수 있는 패턴들입니다.

---

### 1. 쉘 연산자의 실무적 활용 (Control Operators)

단순한 명령어 실행을 넘어, 조건부 실행과 흐름 제어를 가능하게 하는 연산자들입니다.

| 연산자 | 기능 | 실무 활용 예시 (Use Case) |
| --- | --- | --- |
| **`;`** | **순차 실행** | `mkdir backup; cp *.log backup/; tar -cvf backup.tar backup/` (결과에 상관없이 명령 연속 실행) |
| **`&&`** | **성공 시 실행** | `./configure && make && make install` (앞 단계가 성공해야만 다음 단계로 진행) |
| **`||`** | **실패 시 실행** | `grep "CRITICAL" /var/log/app.log |
| **`&`** | **백그라운드 실행** | `python3 long_task.py &` (터미널을 점유하지 않고 백그라운드에서 작업 수행) |
| **`()`** | **서브쉘 그룹화** | `(cd /tmp && ls); pwd` (괄호 안에서만 디렉토리를 이동하고 실행 후 원래 위치 유지) |

---

### 2. 전문가 수준의 복합 명령어 예제 (Pipeline & Redirection)

파이프라인과 다양한 유틸리티를 결합하여 데이터를 가공하는 고급 기법들입니다.

#### ① 특정 시간대 로그만 추출하여 파일로 저장

```bash
# 14시 00분부터 14시 59분 사이의 로그 중 'ERROR'가 포함된 행만 추출
grep "2026-01-09 14:" application.log | grep "ERROR" > error_14h.txt

```

#### ② 현재 접속량이 가장 많은 IP 주소 상위 5개 추출

```bash
# 웹 서버 로그에서 IP(1번째 필드)를 뽑아 정렬하고 빈도수를 계산
cat access.log | awk '{print $1}' | sort | uniq -c | sort -nr | head -n 5

```

#### ③ 특정 포트를 점유 중인 프로세스 강제 종료 (One-liner)

```bash
# 8080 포트를 사용하는 PID를 찾아 즉시 kill
kill -9 $(lsof -t -i:8080)

```

#### ④ 100MB 이상의 대용량 파일만 찾아 삭제 전 목록 확인

```bash
# 100M 초과 파일을 찾아 상세 정보를 출력하고 에러는 버림
find / -type f -size +100M -exec ls -lh {} \; 2>/dev/null

```

#### ⑤ 실시간 로그에서 특정 단어 제외하고 보기

```bash
# 실시간 로그를 보되, 불필요한 'DEBUG'나 'INFO' 메시지는 화면에서 가림
tail -f system.log | grep -vE "DEBUG|INFO"

```

#### ⑥ 디렉토리 내 파일들의 특정 문자열 일괄 치환

```bash
# 현재 폴더의 모든 .conf 파일 내 'localhost'를 '192.168.0.10'으로 변경
sed -i 's/localhost/192.168.0.10/g' *.conf

```

#### ⑦ CPU 사용량이 가장 높은 프로세스 3개 확인

```bash
# ps 결과를 cpu 사용률로 정렬하여 상위 3개 추출
ps -eo pid,ppid,cmd,%mem,%cpu --sort=-%cpu | head -n 4

```

#### ⑧ 빈 디렉토리만 찾아 일괄 삭제

```bash
# 비어있는 디렉토리를 찾아 안전하게 삭제
find . -type d -empty -delete

```

#### ⑨ 원격 서버의 용량을 로컬에서 확인하기

```bash
# SSH 접속 후 바로 명령을 내려 결과를 현재 터미널로 가져옴
ssh user@remote-server "df -h /data"

```

#### ⑩ 파일 내용 비교 및 차이점만 저장

```bash
# 두 설정 파일의 차이점을 분석하여 patch용 파일로 저장
diff -u config.old config.new > config.patch

```

---

### 3. 입출력 흐름의 시각적 이해

명령어 실행 시 발생하는 데이터의 흐름(Stream)을 제어하는 리다이렉션 구조입니다.

| 구분 | 스트림 번호 | 기호 | 설명 |
| --- | --- | --- | --- |
| **Standard Input** | 0 | `<` | 명령어의 입력을 키보드가 아닌 파일에서 받음 |
| **Standard Output** | 1 | `>` | 정상 출력 결과를 파일로 저장 (덮어쓰기) |
| **Standard Error** | 2 | `2>` | 에러 발생 시 출력되는 메시지만 파일로 저장 |
| **Combine All** | - | `&>` | 정상 출력과 에러 출력을 모두 한 파일에 저장 |

---

**Next Step: 쉘 스크립트 조건문(if/case)과 반복문(for/while)**

**AWS 관련 주의사항**:
위 명령어 중 `find`나 `grep` 등을 루트(/) 전체 범위에서 실행할 경우, EBS I/O를 과도하게 사용하여 **I/O 처리량에 따른 추가 비용**이 발생하거나 인스턴스 성능 저하(T 시리즈 프리티어의 경우 CPU 크레딧 소진)가 발생할 수 있으니 주의가 필요합니다.
