## 📁 우분투 리눅스 파일 시스템 및 주요 디렉터리 역할 분석

우분투 리눅스의 파일 시스템은 **FHS (Filesystem Hierarchy Standard)**를 따르며, 모든 파일과 디렉터리는 **루트 디렉터리(`/`)** 아래에 트리(Tree) 구조로 존재합니다.


### 1. 🌲 파일 시스템의 뿌리: `/` (Root Directory)

모든 디렉터리의 시작점입니다. 절대 경로를 지정할 때 항상 `/`로 시작합니다. 

### 2. 🗄️ 시스템의 핵심 바이너리 및 설정 디렉터리

시스템의 작동에 필수적인 명령어 파일과 설정 파일이 저장됩니다.

| 디렉터리 | 영어 명칭 | 주요 역할 및 쓰임 |
| :--- | :--- | :--- |
| **`/bin`** | Binaries | **모든 사용자**가 사용하는 기본적인 실행 파일(명령어)들이 저장됩니다. (예: `ls`, `cp`, `mv`, `cat`) |
| **`/sbin`** | System Binaries | **시스템 관리자(`root`)**가 주로 사용하는 시스템 관리 및 유지보수 명령어들이 저장됩니다. (예: `fdisk`, `reboot`, `ifconfig` - `ip` 명령어는 `/sbin` 또는 `/usr/sbin`에 위치) |
| **`/etc`** | Et Cetera | 시스템의 **모든 설정 파일**이 저장됩니다. (예: `/etc/passwd`, `/etc/fstab`, `/etc/ssh/sshd_config`) |
| **`/lib`** | Libraries | `/bin` 및 `/sbin`에 있는 프로그램들이 사용하는 **핵심 공유 라이브러리 파일**이 저장됩니다. |

### 3. 💾 가변 데이터 및 서버 관리 디렉터리

시스템 운영 중 내용이 자주 바뀌거나, 서버 데이터가 저장되는 디렉터리입니다.

| 디렉터리 | 영어 명칭 | 주요 역할 및 쓰임 |
| :--- | :--- | :--- |
| **`/var`** | Variable Data | 시스템 실행 중 **내용이 계속해서 변하는(가변적인)** 데이터가 저장됩니다. |
| `ㄴ /var/log` | | 시스템 로그 파일이 저장됩니다. (예: 시스템 오류, 서비스 접속 기록) |
| `ㄴ /var/www` | | 웹 서버(Apache, Nginx)의 **웹 콘텐츠(HTML, PHP 등)**가 기본적으로 저장되는 위치입니다. |
| `ㄴ /var/spool` | | 인쇄 대기열(Queue), 메일 전송 대기열 등 임시 저장 데이터가 저장됩니다. |
| **`/tmp`** | Temporary | 시스템 및 프로그램이 **임시로 사용하는 파일**이 저장됩니다. (시스템 재부팅 시 내용이 삭제될 수 있으므로 영구적 사용 금지) |

### 4. 👤 사용자 홈 디렉터리 및 데이터

사용자 개인 파일 및 외장 장치와 관련된 디렉터리입니다.

| 디렉터리 | 영어 명칭 | 주요 역할 및 쓰임 |
| :--- | :--- | :--- |
| **`/home`** | Home Directories | **일반 사용자들의 개인 홈 디렉터리**가 위치합니다. (예: `/home/master`) |
| **`/root`** | Root Home Directory | **최고 관리자(`root`) 계정의 홈 디렉터리**입니다. 일반 사용자의 `/home`과는 분리되어 있습니다. |
| **`/mnt`** | Mount | **임시로 파일 시스템을 마운트**하기 위해 사용됩니다. (과거 CD-ROM, 외장 하드 등) |
| **`/media`** | Media | CD-ROM, USB 드라이브 등 **이동식 미디어를 자동으로 마운트**하기 위해 사용됩니다. (최신 시스템에서 주로 사용) |

### 5. 🖥️ 시스템 커널 및 장치 파일 디렉터리

시스템 하드웨어 제어 및 커널 관련 정보가 저장됩니다.

| 디렉터리 | 영어 명칭 | 주요 역할 및 쓰임 |
| :--- | :--- | :--- |
| **`/dev`** | Devices | **하드웨어 장치 파일**이 위치합니다. 모든 하드웨어 장치(디스크, 터미널, 키보드 등)는 파일 형태로 표현되어 접근됩니다. (예: `/dev/sda`, `/dev/null`) |
| **`/proc`** | Processes | 실행 중인 **커널 및 프로세스에 대한 정보**가 파일 형태로 저장되는 **가상 파일 시스템**입니다. (시스템 메모리에 존재) |
| **`/sys`** | System | 커널의 **장치 트리, 드라이버, 하드웨어 설정 등**을 제어하는 정보를 담는 **가상 파일 시스템**입니다. |
| **`/boot`** | Boot | **리눅스 커널 이미지 파일**과 부트로더(GRUB) 설정 파일이 저장됩니다. 시스템 부팅에 필수적인 파일들입니다. |

### 6. 🔗 보조 실행 파일 디렉터리

| 디렉터리 | 영어 명칭 | 주요 역할 및 쓰임 |
| :--- | :--- | :--- |
| **`/usr`** | Unix System Resources | **사용자에게 제공되는 대부분의 프로그램 및 파일**이 저장됩니다. (읽기 전용의 보조 데이터) |
| `ㄴ /usr/bin` | | 사용자용 실행 파일 |
| `ㄴ /usr/lib` | | 프로그램 라이브러리 |
| `ㄴ /usr/local` | | **사용자가 직접 설치한 프로그램**을 저장하는 곳입니다. 패키지 관리자가 아닌 수동 설치 시 이 경로를 권장합니다. |

이 디렉터리 구조를 이해하는 것은 리눅스에서 파일 찾기, 설정 변경, 서비스 디버깅 등을 수행할 때 매우 중요한 기본 지식입니다.


---
<요약>

리눅스의 파일 시스템은 윈도우와 달리 모든 파일과 디렉토리가 최상위 루트(`/`)를 기점으로 가지를 치는 **역트리(Inverted Tree) 구조**를 가지고 있습니다. 효율적인 서버 관리와 보안을 위해 각 폴더의 역할이 엄격히 구분되어 있습니다.

---

## 1. 리눅스 파일 시스템 계층 구조 (FHS)

리눅스 표준인 **FHS(Filesystem Hierarchy Standard)**에 따른 주요 디렉토리의 역할입니다.

| 디렉토리 | 설명 및 역할 |
| --- | --- |
| **`/` (Root)** | 최상위 디렉토리. 모든 경로의 시작점입니다. |
| **`/bin`** | 시스템 운영에 필수적인 **기본 명령어**들이 위치합니다. (예: `ls`, `cp`, `mv`, `pwd`) |
| **`/etc`** | 시스템의 **환경 설정 파일**들이 모여 있는 곳입니다. (예: 네트워크 설정, 사용자 계정 등) |
| **`/home`** | 일반 사용자들의 **개인 홈 디렉토리**가 생성되는 곳입니다. (예: `/home/user1`) |
| **`/root`** | 시스템 관리자(**root**) 전용 홈 디렉토리입니다. (`/`와는 다름) |
| **`/var`** | 로그 파일, 데이터베이스 등 내용이 **가변적인 파일**들이 저장됩니다. |
| **`/usr`** | 일반 사용자가 설치한 프로그램과 라이브러리가 위치합니다. (윈도우의 'Program Files'와 유사) |
| **`/dev`** | 하드 디스크, 마우스 등 **하드웨어 장치**들을 파일 형태로 관리합니다. |
| **`/tmp`** | 시스템 사용 중에 발생하는 **임시 파일**들을 저장하며, 부팅 시 삭제될 수 있습니다. |

---

## 2. 절대경로 vs 상대경로 (Path Understanding)

원하는 파일이나 폴더로 이동하기 위해 경로를 표현하는 방식은 두 가지가 있습니다.

### **① 절대경로 (Absolute Path)**

* **정의:** 최상위 루트(`/`)를 기준으로 파일의 위치를 나타내는 **고유한 주소**입니다.
* **특징:** 현재 내가 어디에 있든 상관없이 항상 동일한 위치를 가리킵니다.
* **표기:** 반드시 `/`로 시작합니다.
* **예시:** `/home/user/docs/readme.txt`

### **② 상대경로 (Relative Path)**

* **정의:** 현재 내가 작업 중인 위치(**PWD**, Present Working Directory)를 기준으로 파일의 위치를 나타냅니다.
* **특징:** 내가 있는 폴더가 바뀌면 경로의 의미도 달라집니다.
* **표기 기호:**
* **`.` (마침표 하나):** 현재 내 위치
* **`..` (마침표 둘):** 상위(부모) 디렉토리


* **예시:** * 현재 위치가 `/home/user` 일 때, `cd docs` (하위 폴더 이동)
* 현재 위치가 `/home/user/docs` 일 때, `cd ..` (상위 `/home/user`로 이동)



---

## 3. [학습 포인트 및 주의사항]

* **리눅스 환경의 통일성:** 리눅스는 윈도우와 달리 `C:`, `D:` 같은 드라이브 개념이 없습니다. 추가 하드디스크를 설치하더라도 특정 폴더(예: `/mnt` 등)에 연결(**Mount**)하여 사용합니다.
* **대소문자 구분:** 리눅스 파일 시스템은 대소문자를 엄격하게 구분합니다. `Readme.txt`와 `readme.txt`는 서로 다른 파일임을 명심하십시오.
* **[유료 과금 주의]:** AWS 등 클라우드 환경에서 리눅스 파일 시스템을 다룰 때, `/var/log`나 `/tmp` 용량이 꽉 차서 시스템이 멈추면 **인스턴스 복구 비용**이 발생하거나 서비스 장애로 이어질 수 있으므로 정기적인 용량 관리가 필요합니다.

Next Step: **리눅스 기본 명령어(ls, cd, mkdir, rm) 사용법 실습**
