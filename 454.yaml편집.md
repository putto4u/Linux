본 장에서는 Ubuntu 및 클라우드 인프라 관리의 핵심인 **YAML(YAML Ain't Markup Language)** 파일의 개념과 작성 규칙, 그리고 실무에서 실수를 줄이는 최적의 작성 팁을 다룹니다.

---

## 1. YAML 파일의 이해와 특징

YAML은 '사람이 읽기 쉬운' 데이터 직렬화 언어입니다. 주로 설정 파일(Configuration)에 사용되며, Netplan, Docker Compose, Kubernetes, Ansible 등 대부분의 최신 IT 인프라 도구가 이 형식을 채택하고 있습니다.

### 주요 특징

* **가독성**: XML이나 JSON에 비해 구조가 단순하고 사람이 읽고 쓰기 편합니다.
* **계층 구조**: 들여쓰기를 통해 데이터 간의 부모-자식 관계를 표현합니다.
* **엄격한 규칙**: 들여쓰기 한 칸 차이로 설정이 완전히 달라질 수 있어 정교한 작성이 요구됩니다.

---

## 2. YAML 작성의 5대 핵심 규칙

YAML 파일을 실행하거나 적용할 때 발생하는 에러의 90% 이상은 아래 규칙을 어겼을 때 발생합니다.

### ① 들여쓰기 (Indentation)

* **반드시 공백(Space)을 사용**해야 합니다.
* **탭(Tab) 문자는 절대 금지**입니다. (Netplan 적용 시 에러의 주범)
* 보통 **2칸 또는 4칸**을 사용하며, 같은 계층(Level)은 반드시 들여쓰기 칸수가 일치해야 합니다.

### ② 데이터 표현 (Key: Value)

* `Key: Value` 형태로 작성하며, **콜론(:) 뒤에는 반드시 한 칸의 공백**이 있어야 합니다.
* (잘못된 예) `ip:192.168.0.1`
* (올바른 예) `ip: 192.168.0.1`



### ③ 리스트 (List)

* 여러 항목을 나열할 때는 하이픈(`-`)을 사용합니다. 하이픈 뒤에도 반드시 공백이 필요합니다.

```yaml
addresses:
  - 192.168.1.10/24
  - 192.168.1.11/24

```

### ④ 주석 (Comment)

* 샵(`#`) 기호를 사용하여 코드에 설명을 추가할 수 있습니다. 해당 줄은 시스템에 의해 무시됩니다.

### ⑤ 시작과 끝 (Optional)

* 문서의 시작을 `---`(하이픈 3개)로 표시하고, 끝을 `...`(마침표 3개)로 표시하기도 하지만, 단일 설정 파일에서는 생략하는 경우가 많습니다.

---

## 3. 실무 효율을 높이는 YAML 편집 팁

### 정적 분석 (Linting)

파일을 적용하기 전, 문법적 오류가 없는지 미리 검사하는 습관이 중요합니다.

* **온라인 도구**: YAML Lint 등의 사이트 활용
* **CLI 도구**: `yamllint` 패키지 설치 및 검사
```bash
sudo apt install yamllint
yamllint 01-netcfg.yaml

```



### 터미널 편집기 설정 (Vim)

Vim에서 탭을 눌러도 공백으로 인식하게 하고, 열 맞춤을 시각적으로 도와주는 설정입니다.

1. `vi ~/.vimrc` 실행
2. 아래 내용 추가:

```vim
autocmd FileType yaml setlocal ts=2 sts=2 sw=2 expandtab
set cursorcolumn  " 현재 커서의 세로 열을 표시하여 계층 확인 도움

```

---

## 4. Netplan YAML 예시 (복습)

앞서 배운 규칙이 실제 네트워크 설정에 어떻게 적용되는지 확인해 보세요.

```yaml
network:           # (1) 최상위 계층
  version: 2       # (2) 버전 정보
  renderer: networkd
  ethernets:       # (3) 하위 계층 (2칸 들여쓰기)
    eth0:          # (4) 상세 항목 (다시 2칸 들여쓰기)
      dhcp4: no
      addresses:   # (5) 리스트 시작
        - 192.168.0.10/24
      routes:
        - to: default
          via: 192.168.0.1

```

---

Next Step: JSON과 YAML의 차이점 및 상호 변환 방법
