이 가이드는 **Ubuntu 18.04 LTS 이후 버전**(20.04, 22.04, 24.04 등)에서 네트워크 설정 도구인 **Netplan**을 사용하여 정적 IP(Static IP)를 할당하는 방법을 설명합니다.

---

## 1. 네트워크 인터페이스 이름 확인

먼저 IP를 고정할 네트워크 인터페이스의 정확한 이름을 확인해야 합니다.

```bash
ip addr

```

* 결과 예시: `ens33`, `eth0`, `enp0s3` 등 (시스템마다 다를 수 있습니다.)

---

## 2. Netplan 설정 파일 편집

Netplan 설정 파일은 `/etc/netplan/` 디렉토리에 `.yaml` 확장자로 존재합니다.

### 설정 파일 위치 확인

```bash
ls /etc/netplan/

```

* 보통 `01-netcfg.yaml`, `50-cloud-init.yaml` 또는 `00-installer-config.yaml`과 같은 이름으로 되어 있습니다.

### 파일 편집

기존 설정 파일을 열거나, 새 파일을 생성하여 편집합니다. (여기서는 `00-installer-config.yaml`을 예로 듭니다.)

```bash
sudo nano /etc/netplan/00-installer-config.yaml

```

---

## 3. 정적 IP 상세 설정 (YAML)

파일 내용을 다음과 같이 수정합니다. **중요: YAML 형식은 들여쓰기(스페이스 2칸)가 매우 엄격하므로 주의하십시오.**

```yaml
network:
  version: 2
  renderer: networkd
  ethernets:
    ens33:                # 위 1번에서 확인한 인터페이스 이름
      dhcp4: no           # IPv4 DHCP 비활성화
      addresses:
        - 192.168.1.100/24 # 설정하려는 고정 IP와 서브넷 마스크
      routes:
        - to: default
          via: 192.168.1.1 # 게이트웨이(공유기/라우터) IP
      nameservers:
        addresses: [8.8.8.8, 8.8.4.4] # DNS 서버 (구글 DNS 예시)

```

---

## 4. 설정 적용 및 확인

작성한 설정을 시스템에 적용하기 전에 구문 오류가 없는지 테스트합니다.

### 설정 테스트

```bash
sudo netplan try

```

* 오류가 없다면 `Enter`를 눌러 확정합니다. 문제가 생기면 2분 후 자동으로 이전 설정으로 복구됩니다.

### 설정 즉시 적용

```bash
sudo netplan apply

```

### 최종 확인

변경한 IP가 정상적으로 할당되었는지 확인합니다.

```bash
ip addr show ens33
ip route

```
