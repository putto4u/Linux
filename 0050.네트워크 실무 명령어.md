네트워크 실무에서 현재 통신 상태와 소켓 정보를 확인하는 것은 문제 해결의 핵심입니다. 기존에 널리 쓰이던 `netstat`과 이를 대체하는 고성능 도구인 `ss`를 중심으로, 실무에서 바로 사용할 수 있는 주요 옵션과 조합을 정리합니다.

---

## 네트워크 소켓 상태 확인 (`ss` 또는 `netstat`)

최근 리눅스 배포판에서는 더 빠르고 상세한 정보를 제공하는 `ss` 사용을 권장하지만, 많은 인프라 환경에서 여전히 `netstat`이 사용되고 있습니다. 두 명령어는 옵션 구성이 유사하여 함께 익혀두면 유용합니다.

### 1. 모든 수신 대기 소켓 확인 (Listening Ports)

서버에 어떤 포트가 열려 있는지, 특정 서비스가 정상적으로 올라왔는지 확인할 때 사용합니다.

* **명령어:** * `ss -tunlp`
* 또는 `netstat -tunlp`


* **주요 옵션:**
* `-t`: TCP 소켓만 표시
* `-u`: UDP 소켓만 표시
* `-n`: 호스트/서비스 이름 대신 숫자(IP/포트)로 표시 (속도 빠름)
* `-l`: Listening 상태인 소켓만 표시
* `-p`: 해당 소켓을 사용하는 프로세스 정보(PID/이름) 표시



### 2. 특정 포트 점유 상태 확인

특정 서비스(예: 웹 80, SSH 22)가 동작 중인지 확인하거나, 포트 충돌 원인을 찾을 때 사용합니다.

* **실무 조합:**
* `ss -atn | grep :80`
* 또는 `netstat -atn | grep :80`


* **설명**: `-a` 옵션을 추가하면 연결된 상태(Established)와 대기 상태(Listening)를 모두 포함하여 검색합니다.

---

## 인터페이스 및 경로 관리 (`ip` 또는 `ifconfig`/`route`)

네트워크 인터페이스의 IP 설정과 라우팅 경로를 확인하는 도구입니다.

### 3. IP 주소 및 인터페이스 상태 확인

* **명령어:**
* `ip addr`
* 또는 `ifconfig -a`


* **실무 팁:** `ip -c addr`을 사용하면 결과에 색상이 입혀져 인터페이스 상태(UP/DOWN)를 더 쉽게 식별할 수 있습니다.

### 4. 라우팅 테이블(게이트웨이) 확인

패킷이 나가는 경로와 기본 게이트웨이 설정을 확인합니다.

* **명령어:**
* `ip route`
* 또는 `route -n` (또는 `netstat -rn`)


* **실무 활용:** `ip route get [목적지IP]`를 실행하면 해당 IP로 갈 때 실제 어떤 인터페이스와 경로를 사용하는지 미리 계산해 볼 수 있어 장애 조치에 탁월합니다.

---

## 고급 실무 활용: 네트워크 연결 통계

### 5. 프로토콜별 통계 정보

네트워크 에러, 재전송(Retransmit) 패킷 등 전반적인 통계를 확인하여 품질을 진단합니다.

* **명령어:**
* `ss -s` (요약된 통계 정보 출력)
* 또는 `netstat -s` (상세한 프로토콜별 통계)



### 6. ARP 테이블(이웃 노드) 확인

동일 네트워크 대역 내에 있는 장비들의 IP와 MAC 주소 매핑 정보를 확인합니다.

* **명령어:**
* `ip neigh` (또는 `ip neighbor`)
* 또는 `arp -an`



---

## 요약: 실무 권장 명령어 변환표

| 목적 | 기존 방식 (Deprecated) | 현대적 방식 (Recommended) |
| --- | --- | --- |
| **IP/인터페이스 확인** | `ifconfig` | `ip addr` |
| **라우팅 테이블** | `route -n` | `ip route` |
| **소켓/포트 확인** | `netstat -tnlp` | `ss -tnlp` |
| **ARP 확인** | `arp -a` | `ip neigh` |
| **멀티캐스트 확인** | `netstat -g` | `ip maddr` |

> **실무 주의사항**
> 현대적인 리눅스(Ubuntu 18.04+, RHEL 7+)에서는 `net-tools`(ifconfig, netstat 등) 패키지가 기본 설치에서 제외되는 추세입니다. 가급적 `iproute2` 패키지에 포함된 `ip`와 `ss` 명령어 사용에 익숙해지는 것이 좋습니다.

---

네트워크 인터페이스의 활성화(UP), 비활성화(DOWN), 그리고 설정된 IP나 인터페이스 자체를 삭제하는 작업은 실무 장애 대응 시 가장 빈번하게 수행하는 작업입니다. 기존의 `ifconfig` 방식과 최신 `ip` 명령어를 병행하여 정리합니다.

---

## 1. 인터페이스 활성화 및 비활성화 (UP / DOWN)

물리적 또는 논리적 인터페이스의 상태를 제어합니다.

* **명령어:**
* `ip link set [인터페이스명] up/down`
* 또는 `ifconfig [인터페이스명] up/down`


* **실무 활용:**
```bash
# eth0 인터페이스 비활성화
sudo ip link set eth0 down
# 또는
sudo ifconfig eth0 down

# eth0 인터페이스 활성화
sudo ip link set eth0 up
# 또는
sudo ifconfig eth0 up

```



---

## 2. IP 주소 할당 및 삭제 (Add / Delete)

인터페이스에 임시로 IP를 부여하거나, 잘못 설정된 IP를 즉시 제거할 때 사용합니다. (주의: 이 명령어로 수정된 사항은 재부팅 시 초기화되므로 영구 적용은 Netplan YAML 파일을 수정해야 합니다.)

### IP 추가 (Add)

* **명령어:**
* `ip addr add [IP/서브넷] dev [인터페이스명]`
* 또는 `ifconfig [인터페이스명] [IP] netmask [서브넷]`


* **실무 활용:**
```bash
# eth0에 192.168.1.50 IP 추가 할당
sudo ip addr add 192.168.1.50/24 dev eth0

```



### IP 삭제 (Delete)

* **명령어:**
* `ip addr del [IP/서브넷] dev [인터페이스명]`


* **실무 활용:**
```bash
# eth0에서 특정 IP 삭제
sudo ip addr del 192.168.1.50/24 dev eth0

```



---

## 3. 라우팅 경로 추가 및 삭제 (Route)

특정 네트워크 대역으로 가는 길(Gateway)을 수동으로 잡거나 삭제합니다.

### 경로 추가

* **명령어:**
* `ip route add [네트워크대역] via [게이트웨이IP] dev [인터페이스명]`
* 또는 `route add -net [대역] gw [게이트웨이IP]`


* **실무 활용:**
```bash
# 10.10.10.0 대역으로 가는 경로를 192.168.1.1로 지정
sudo ip route add 10.10.10.0/24 via 192.168.1.1 dev eth0

```



### 경로 삭제

* **명령어:**
* `ip route del [네트워크대역]`
* 또는 `route del -net [대역]`


* **실무 활용:**
```bash
# 등록된 특정 경로 삭제
sudo ip route del 10.10.10.0/24

```



---

## 4. 인터페이스 초기화 (Flush)

특정 인터페이스에 설정된 모든 IP 주소와 설정값을 한 번에 밀어버리고 싶을 때 사용합니다. `ip` 명령어만의 강력한 기능입니다.

* **명령어:** `ip addr flush dev [인터페이스명]`
* **실무 활용:**
```bash
# eth0에 할당된 모든 IP 설정을 초기화
sudo ip addr flush dev eth0

```



---

## 요약 및 비교표

| 작업 | 현대적 방식 (`ip` 명령어) | 기존 방식 (`net-tools`) |
| --- | --- | --- |
| **인터페이스 ON** | `ip link set eth0 up` | `ifconfig eth0 up` |
| **인터페이스 OFF** | `ip link set eth0 down` | `ifconfig eth0 down` |
| **IP 추가 할당** | `ip addr add 1.1.1.1/24 dev eth0` | `ifconfig eth0 1.1.1.1 ...` |
| **IP 삭제** | `ip addr del 1.1.1.1/24 dev eth0` | 없음 (IP 변경만 가능) |
| **경로(Route) 삭제** | `ip route del 10.10.10.0/24` | `route del -net 10.10.10.0 ...` |
| **전체 초기화** | `ip addr flush dev eth0` | 없음 |

---

Next Step: 본딩(Bonding) 인터페이스의 동적 구성 해제 및 멤버 인터페이스 관리법
